Class {
	#name : #DRRangeConstraint,
	#superclass : #DRSimpleConstraint,
	#category : #'Druid-Optimizations'
}

{ #category : #testing }
DRRangeConstraint >> hasAllValuesWithMask: aMask [

	^ false "always?"
]

{ #category : #testing }
DRRangeConstraint >> hasAllValuesWithoutMask: aMask [

	^ false "always?"
]
