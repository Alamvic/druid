SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'Druid',
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming,
      #ignoreImage : true,
      #platforms : [ #pharo ]
    }
  ]
}
