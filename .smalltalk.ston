SmalltalkCISpec {
  #preLoading : 'ci/preLoading.st',
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'Druid',
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming,
      #ignoreImage : true,
      #platforms : [ #pharo ]
    }
  ]
}
